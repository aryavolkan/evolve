# DEPRECATED: This workflow is broken. Use tests.yml instead.
name: Test (DEPRECATED)

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  godot-tests:
    name: Godot Tests
    runs-on: ubuntu-latest
    if: false  # Disabled - use tests.yml instead
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Run tests
        run: echo "Deprecated - use tests.yml"
